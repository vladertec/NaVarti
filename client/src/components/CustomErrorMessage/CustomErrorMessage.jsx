const CustomErrorMessage = ({ name }) => {
    return (
      <div className="custom-error">
        <span className="custom-error__text">{name}</span>
      </div>
    )
  }
  export default CustomErrorMessage